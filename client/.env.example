# Local development
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000

# Production / EC2 (same-origin behind nginx) — used at build time:
# VITE_API_URL=/api
# VITE_SOCKET_URL=
